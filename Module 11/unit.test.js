import {unit} from "./unit.js";

describe(blockName: "tests for definePrime, function", block()=>{
    const simple = 17;
    const complex = 18;

    It (testName: "should operate correctly with simple number", fn() =>{
        expect (definePrime (simle)).toBe(expected:`Число${simple} - простое число`);
    })
})